import styled from "styled-components";

const RangeWrapper = styled.div`
  color: white;
`;

function Range() {
  return (
    <RangeWrapper>
      <p>
        Betwen 1 and 20
      </p>
    </RangeWrapper>
  );
}

export default Range;
